# Build target for the docker-build smoke test, kept in a subdirectory so the
# test can exercise the `context` and `dockerfile` inputs. Same dummy
# echo-server as the root Dockerfile.
FROM ealen/echo-server:latest
ENV PORT=3000
